#pragma once
#include <cmath>
#include <numeric>
namespace sci::stat::host {

template <typename T> T vars1D_with_cpu(T *h_in, const unsigned int N) {}
// T std_with_cpu(T *h_in, const usize_t lda, const usize_t);
